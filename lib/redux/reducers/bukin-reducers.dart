import 'package:bukin_app/redux/stores/bukin-state.dart';

BukinState bukinStateReducer (BukinState prevState, {String action, dynamic payload}) {

  switch (action) {
    case '':
      
      break;
    default:
  }

  return prevState;
}